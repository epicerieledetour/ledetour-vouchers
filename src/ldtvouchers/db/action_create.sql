INSERT INTO actions (
    origin,
    req_usertoken,
    req_vouchertoken,
    userid,
    voucherid,
    requestid
)
VALUES (
    :origin,
    :req_usertoken,
    :req_vouchertoken,
    :userid,
    :voucherid,
    :requestid
)
